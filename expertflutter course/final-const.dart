// example 1
/* void main() {
  final myName = 'Saman khoobi';

  print(myName.length);
}
 */

// example 2
/* void main() {
  final name = 'Amir';
  final nameLength = name.length;
  print(nameLength);

  const productName = 'pillow';
  const productNameLength = productName.length; // It is possible because productName is a constant variable.
  print(productNameLength);

  final lastName = 'miri';
  const lastNameLength = lastName.length; // error: const variable(lastNameLength) must be initialized with a constant value.
  print(productNameLength);
}
 */

// example 3
